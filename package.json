{
  "name": "@nft360/nft360-storage-host",
  "version": "0.1.0",
  "description": "Storage host for Nft360 Network",
  "publishConfig": {
    "access": "public"
  },
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build:types": "node scripts/generate.js",
    "lint": "eslint . --ext js --ext ts",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rimraf dist",
    "prepublish": "npm run -s clean && npm run -s build"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@babel/runtime": "^7.14.6",
    "@types/node": "^14.14.31",
    "@typescript-eslint/eslint-plugin": "^4.6.1",
    "@typescript-eslint/parser": "^4.6.1",
    "eslint": "^7.13.0",
    "typescript": "^4.0.3"
  }
}
